- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.config:
        create: true
        path: config/*
        relink: true
        glob: true

- create:
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
